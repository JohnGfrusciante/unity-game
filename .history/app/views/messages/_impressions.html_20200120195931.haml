%li
  .userName
    = @user.name
    - @items.each do |item|
      - item.item_images.first(1).each do |img|
  .date
    2019年12月12日 22:30
    -# = messages.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .message
    -# 良いね！
    -# = @messages
    = @messages