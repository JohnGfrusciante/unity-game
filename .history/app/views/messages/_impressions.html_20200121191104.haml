- @messages.each do |message|
  - @users.each do |user|
    %li
      .userName
        = user.name
      .date
        = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
      .message
        = message.text